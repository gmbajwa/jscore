'use strict'
/**
 * Sum of first n natural numbers using While Loop
 */
console.log(`/**
 * Sum of first n natural numbers using While Loop
 */`)
console.log(`
    
    
    `)
let num = 0
let sum = 0

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

sum = sum + num 
num++

console.log(`Sum of first ${num} natural numbers is : ${sum}`)



// Some of first N Natural Numbers using Mathametical Formula (n*(n+1))/2
let sum3 = 0
let limit = 9999999999
sum3 = (limit*(limit + 1))/2
console.log(`Sum of first ${limit} natural numbers is : ${sum3}`)

// Some of first N Natural Numbers using While Loop
let sum2 = 0
let counter = 0
while(counter <= 9){
    sum2 = sum2 + counter
    counter++
}

console.log(`Sum of first ${counter} natural numbers is : ${sum2}`)

console.log(`
    
    
    `)    
/**
 * Errors In JS
 * 
 * 1- type of 'NON' is an Object
 * 
 * 2- 'use stict' mode is not working if we use it
 * in the middle or in the bottom of file
 * with out keyword and 'use strict' mode if we
 * 
 * 3- declare any veriable with out any keyword
 * its not possible  
 */

console.log(`/**
* Errors In JS
* 
* 1- type of 'NON' is an Object
* 
* 2- 'use stict' mode is not working if we use it
* in the middle or in the bottom of file
* with out keyword and 'use strict' mode if we
* 
* 3- declare any veriable with out any keyword
* its not possible  
*/`);