'use strict'
/**
 * While Loop in Array Data Type
 */
console.log(`/**
 * While Loop in Array Data Type
 */`)
console.log(`
While Loop in Array Data Type
============================= 
/**
 * Start Coding From Here
 */

While Loop in Array Data Type
=============================
    
    
`)
    
/**
 * Start Coding From Here
 */
const fruits = ['Apple', 'Banana', 'Mango', 'Orange', 'Pineapple'];
let i = 0;
while (i < fruits.length) {
    console.log(fruits[i]);
    i++;
} 

// Use Case, create a new array of fruites with Uppercase

const fruitsNames = []
let j = 0;
while(j < fruits.length){
    fruitsNames.push(fruits[j].toUpperCase())
    j++
} 

console.log(fruitsNames)

/**
 * Summary
 * 
 * 1- While loop is used to iterate over array data type
 * 2- We have to declare and initialize the counter variable
 * before using it in while loop
 * 3- We have to increment the counter variable inside the loop
 * otherwise it will create infinite loop
 */

console.log(`
    
    
`)    
/**
 * Errors In JS
 * 
 * 1- type of 'NON' is an Object
 * 
 * 2- 'use stict' mode is not working if we use it
 * in the middle or in the bottom of file
 * with out keyword and 'use strict' mode if we
 * 
 * 3- declare any veriable with out any keyword
 * its not possible  
 */

console.log(`/**
* Errors In JS
* 
* 1- type of 'NON' is an Object
* 
* 2- 'use stict' mode is not working if we use it
* in the middle or in the bottom of file
* with out keyword and 'use strict' mode if we
* 
* 3- declare any veriable with out any keyword
* its not possible  
*/`);