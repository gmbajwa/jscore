'use strict'
/**
 * String Indexing
 * 1- Learn string are immuteable
 * 2- Learn string type has length property
 * 3- Learn string index start form 0
 * 4- Learn string last character index is length-1
 */
console.log(`/**
 * String Indexing
 * 1- Learn string are immuteable
 * 2- Learn string type has length property
 * 3- Learn string index start form 0
 * 4- Learn string last character index is length-1
 */`)
var myFullName = 'Mustafa Ibrahemi'
// Strings Type has length property

console.log(`length of myFullName:`, myFullName.length)

/**
 * print the last character of 'myFullName' 
 * as string index start from 0, so the
 * last characer has index of 'length-1'
 */
console.log(`first characterof myFullName:`, myFullName[0])
console.log(`last character of myFullName:`, myFullName[myFullName.length - 1])
console.log(`second last character of myFullName:`, myFullName[myFullName.length - 2])
console.log(`third last character of myFullName:`, myFullName[myFullName.length - 3])



console.log(`
    
    
    `)    
/**
 * Errors In JS
 * 
 * 1- type of 'NON' is an Object
 * 
 * 2- 'use stict' mode is not working if we use it
 * in the middle or in the bottom of file
 * with out keyword and 'use strict' mode if we
 * 
 * 3- declare any veriable with out any keyword
 * its not possible  
 */

console.log(`/**
* Errors In JS
* 
* 1- type of 'NON' is an Object
* 
* 2- 'use stict' mode is not working if we use it
* in the middle or in the bottom of file
* with out keyword and 'use strict' mode if we
* 
* 3- declare any veriable with out any keyword
* its not possible  
*/`);